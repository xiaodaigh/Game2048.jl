DO NOT DELETE this folder

this contains an the version of 2048 not using bitboard and just using arrays.

But the simpler array version is used to generate the lookup for efficient bitboard moves.

Hence it must not be removed
